/**
 * Defines the types of property fields that can
 * exist on a model definition
 *
 * @author Asa Schachar (asa@optimizely.com)
 */

/**
 * For the apiActions.getAll and getPage methods these must match
 * the key in sort.functions[key]
 */
module.exports = {
  DATE: 'date',
  NUMBER: 'number',
  STRING: 'string',
  BOOLEAN: 'boolean',
  ARRAY: 'array',
  OBJECT: 'object',
}
